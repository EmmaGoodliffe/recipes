{
  "$id": "schema:Recipe",
  "title": "Recipe",
  "type": "object",
  "allOf": [{ "$ref": "#/$defs/Parent" }],
  "properties": { "rec": { "type": "string" } },
  "$defs": {
    "Parent": {
      "$id": "schema:Parent",
      "title": "Parent",
      "type": "object",
      "properties": {
        "par": { "anyOf": [{ "type": "string" }, { "$ref": "#/$defs/Butt" }] }
      }
    },
    "Butt": {
      "$id": "schema:Butt",
      "title": "Butt",
      "type": "object",
      "properties": {
        "but": { "anyOf": [{ "type": "string" }, { "$ref": "#/$defs/Parent" }] }
      }
    }
  }
}
